{
  "name": "coke-lab-dapp",
  "version": "1.0.0",
  "description": "Cardano Coke Lab dApp - Full Stack",
  "private": true,
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix web install",
    "dev": "concurrently \"npm --prefix server run dev\" \"npm --prefix web run dev\"",
    "dev:server": "npm --prefix server run dev",
    "dev:web": "npm --prefix web run dev",
    "build": "npm --prefix web run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

